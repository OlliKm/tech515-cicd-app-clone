# Use this repo with Jenkins

## About the app
- "app" folder stores Sparta app
- uses Node JS v20

```
echo "# 1" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/OlliKm/1.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/OlliKm/1.git
git branch -M main
git push -u origin main
```



# cicd version